# Ralph Loop Progress Log

No stories completed yet. Starting from US-001.
2026-03-04 US-001 Created prompt_engineering.py and react_agentic.py with # /// script inline metadata blocks enabling uvx marimo run without manual environment setup.
2026-03-04 US-002 Confirmed litellm model string UI config panel (mo.ui.text with model/api_key/api_base fields) already implemented in both notebooks from US-001; marked as passing.
2026-03-04 US-003 Added kind="password" to api_key_input in both notebooks so the API key field is masked, enabling secure entry of optional credentials for remote/self-hosted endpoints.
2026-03-04 US-004 Fixed shared re/plt imports in react_agentic.py so all cells depending on call_llm (which depends on config panel variables) re-run automatically when model/api_key/api_base changes.
2026-03-04 US-005 Confirmed individual prompt component toggles (instruction, data, format, persona, context checkboxes) already implemented in Section 2 of prompt_engineering.py; marked as passing.
2026-03-04 US-006 Refactored Section 2 assembled prompt cell to always show a live preview (with note that it updates in real time), keeping preview visible alongside the model response when button is clicked.
2026-03-04 US-007 Fixed Section 2 cell so the assembled prompt is actually sent and the model response is displayed when the button is clicked (assigned mo.vstack to _display variable so marimo renders it).
2026-03-04 US-008 Updated few-shot slider to discrete steps [0,1,2,5] and fixed state management by moving mo.state to a separate cell so results accumulate across runs.
2026-03-04 US-009 Added example order column to results table, zero-shot guard for shuffle button, and warning callout about few-shot brittleness in production.
2026-03-04 US-010 Replaced single toggle with three buttons (run-direct, run-cot, run-both) in Section 4, enabling direct-answer baseline mode and side-by-side comparison with chain-of-thought.
2026-03-04 US-011 Added correct/incorrect flag column to Student Task 2 results table; split mo.state into its own cell so results persist across runs; CoT vs direct-answer comparison now shows ✅/❌ per row.
2026-03-04 US-012 Confirmed Student Task 1 (Gaussian by Prompt) already fully implemented in prompt_engineering.py with prompt editor, run button, automatic number extraction, histogram with N(0,1) PDF overlay, and KS-test p-value with pass/fail indicator; marked as passing.
2026-03-04 US-013 Confirmed extract_numbers() regex function already implemented in prompt_engineering.py (lines 766-769) and called automatically after each LLM response in the Gaussian task; marked as passing.
2026-03-04 US-014 Fixed Gaussian display cell by splitting it into a helper cell (extract_numbers + imports) and a display cell, replacing the broken `return extract_numbers, np, plt, re, stats` that was overriding marimo's cell output so histogram and KS-test now render correctly.
2026-03-04 US-015 Confirmed KS-test p-value with pass/fail indicator already implemented in prompt_engineering.py (lines 790-816) with scipy.stats.kstest, ✅/❌ indicator, and green/red mo.callout; marked as passing.
